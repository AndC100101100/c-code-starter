#include <stdio.h>

int main(){
	char letter = 'M';
	printf("Character %c which is decimal %d\n", letter, letter); // M is 77 in the ascii table and when saved into memory, would go in as binary as 1001101
	return(0);
}
